<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidu.demo2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".BillboardCompassActivity" />
        <activity android:name=".BillboardLandmarksActivity" />
        <activity android:name=".CircleSceneActivity" />
        <activity android:name=".MountainDrawActivity" />
        <activity android:name=".ShapeDrawActivity" />


    </application>>
</manifest>
